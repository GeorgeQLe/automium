# Benchmark Fixtures

This directory contains the frozen v1 benchmark corpus and KPI fixtures used by the benchmark contract tests.

- `corpus.v1.json`
- `kpi-run-outcomes.v1.json`
- `kpi-summary.v1.json`

The manifest references provisioned accounts and seeded environments by stable path, but the private account contents are still a manual provisioning dependency.
