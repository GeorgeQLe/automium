# Contracts Fixtures

This directory contains the frozen v1 golden fixtures used by the contract test suite.

- `semantic-snapshot.v1.json`
- `replay-event.v1.json`
- `planner-adapter.v1.json`

These fixtures are schema examples only. Real benchmark account material and private seed data remain outside the repository and are tracked as manual provisioning work.
